---
description: 提醒用户适时 git commit，避免改动堆积
alwaysApply: true
---

# Git 提交节奏

## 代理职责

在 **DayRecord** 仓库工作时，主动提醒用户何时该 commit。**不要擅自 commit**（除非用户明确要求）。

完成一批有意义改动、或对话/session 将结束前，用 1～2 句话说明：
- 建议现在提交的原因
- 建议的 commit 主题（动词开头，一句说清「为什么」）

用户未要求时，只提醒、不执行 `git commit`。

## 应立即提醒提交的时机

1. **一个完整单元做完**：单个 bug 修复、单个功能、单次重构收尾，且本地可编译/测试通过
2. **改动面变大**：未提交变更涉及 **≥3 个文件**，或同一主题已持续多轮编辑
3. **切换任务前**：从「功能 A」转到不相关的「功能 B」之前
4. **高风险操作前**：大重构、改 CI/发布流程、动数据库/迁移、批量重命名之前——先 commit 当前稳定点
5. **可发布/可演示节点**：README、Release workflow、用户可见行为已对齐时
6. **Session 结束前**：仍有未提交改动时，必须提醒用户决定 commit 还是故意暂留

## 建议拆成多次 commit 的信号

- 同一工作区里混有 **fix + feat + docs + chore**
- diff 里出现 **无关文件**（格式化、锁文件、生成物与业务逻辑缠在一起）
- 一次对话里解决了 **两个以上独立问题**

提醒用户：「可以拆成 2～3 个 commit，方便 review 和回滚。」

## 建议的 commit 粒度（DayRecord）

| 类型 | 示例 |
|------|------|
| `fix:` | GUI 复盘预览、API Key 热重载 |
| `feat:` | MCP resource、新 CLI 子命令 |
| `ci:` | release workflow、三平台构建 |
| `docs:` | README、install-prebuilt |
| `refactor:` | 抽 port、迁 orchestrator |

## 不要提醒提交的情况

- 改动半途、编译/测试仍失败
- 用户明确说「先别提交」「还在实验」
- 仅临时调试代码、注释、本地配置

## 提醒话术模板

> 建议现在 commit：已完成 [X]，涉及 [N] 个文件，主题稳定。  
> 提议信息：`fix: ...` / `feat: ...`  
> 需要我帮你提交的话说一声。
