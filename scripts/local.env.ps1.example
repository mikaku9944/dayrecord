# Copy to scripts/local.env.ps1 (gitignored) for machine-specific env.
# reqwest / DeepSeek HTTP client honors standard proxy variables when set.

# $env:HTTP_PROXY = "http://127.0.0.1:7897"
# $env:HTTPS_PROXY = "http://127.0.0.1:7897"
# $env:NO_PROXY = "localhost,127.0.0.1"
